from typing import Dict, List

item = Shipment((input("Enter shipment item: "), input("Enter size of item in cm: "))


class Shipment:
    """
    Attrs:
        name (str): name of the item
        size (int): in cm
        category (str): category of item (food, )
    """
    def __init__(self, name: str, size: int, category: str):
        self.name = name
        self.size = size
        self.category = category

class Trolly:
    """
    Attrs:

    """
    def __init__(self, size: int):
        self.size = size

    def add_item_to_trolly():

    def scan_item():
        return f"This item is a(n) {Shipment}"

